# CRUD-Mongodb-Elasticsearch-React-Node
This was a project for big data subject. It uses elasticsearch for fast searching of objects and also implements CRUD feature using Node.js, Mongodb and React.
